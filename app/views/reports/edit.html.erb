<% provide(:title, "Prüfbericht anpassen") %>
<h1>Prüfbericht anpassen</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@report) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Speichern", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
